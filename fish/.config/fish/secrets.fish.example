# ============================================================================
# Fish Shell Secrets Template
# Copy to secrets.fish and fill in your values:
#   cp secrets.fish.example secrets.fish
#
# This file is loaded by config.fish if it exists.
# secrets.fish is gitignored — never commit actual tokens.
# ============================================================================

# GitHub Container Registry Personal Access Token
# Generate at: https://github.com/settings/tokens
# Scopes needed: read:packages, write:packages
set -gx CR_PAT ""

# SonarQube / SonarCloud API Token
# Generate at: https://sonarcloud.io/account/security
set -gx SONAR_TOKEN ""

# npm Publish Token (used by ~/.npmrc)
# Generate at: https://www.npmjs.com/settings/~/tokens
set -gx NPM_TOKEN ""
